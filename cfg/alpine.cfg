TAG=latest
REPO=$DISTRO
ARCHS=$ARCHS_DEF
FROM=\${ARCH}\${DISTRO}:\${TAG}

BASE="RUN apk add --no-cache musl-dev glib-dev gcc pkgconfig"
BUILD="RUN apk add --no-cache vala flex bison make autoconf autoconf-archive automake libtool"
